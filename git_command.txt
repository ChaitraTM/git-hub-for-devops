/git init
git add <filename>
git status
git rm --cache <filename>
git commit -m "messsage"
git restore <filename>

git remote add origin https://github.com/ChaitraTM/github-for-devops.git
git remote -v

//local to github
git remote set-url origin https://#token@github.com/ChaitraTM/github-for-devops.git
git remote -v
git push origin master

//update in github the newcode
git add .
git commit -m "message"
git push origin main

//this is new changes
| GitHub → VS Code | `git pull origin main`        | Gets latest code from GitHub       |
| VS Code → GitHub | `git add .`, `commit`, `push` | Sends your local changes to GitHub |






